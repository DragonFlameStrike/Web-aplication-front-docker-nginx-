const React = require("react");

class EmptyFieldComponent extends React.Component{
    render() {
        return (
            <section className="content content_center">
                Choose an action.
            </section>
        )
    }
}

export default EmptyFieldComponent